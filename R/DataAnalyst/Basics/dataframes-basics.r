# Dataframes follow a similar construct to tabular data seen in other programming languages.
# Initial methods are shown below that provide details on the dataframes being reviewed

# Head of the dataframe
head(mtcars)

# Investigate the structure of mtcars - provides high level insights of dataframe structure
str(mtcars)
