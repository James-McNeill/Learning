# order(): is a function that gives you the ranked position of each element when it is applied on a variable

# Create a vector
a <- c(1000, 99, 400000)
order(a)

# console will output the rank position of each element in the vector
